const express = require('express');
const router = express.Router();

// All user-related functionality, including watchlist and registration,
// has been disabled as per the new requirements.

// An empty router is exported to maintain the application's structure
// without providing any user-specific routes.

module.exports = router;
